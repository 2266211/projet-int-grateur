<!DOCTYPE html>
<html lang="en">

<head>
    <title>QuizLab</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!--Add Bootstrap CDN and a link to jQuery-->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <!--Add Bootstrap CDN and a link to jQuery-->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <style>
        body, html {
            height: 100%;
            margin: 0;
        }
    
        .navbar {
            margin-bottom: 0;
            background-color: #0d6efd;
            z-index: 9999;
            border: 0;
            font-size: 12px !important;
            line-height: 1.42857143 !important;
            letter-spacing: 4px;
            border-radius: 0;
        }
    
        .navbar li a,
        .navbar .navbar-brand {
            color: #fff !important;
        }
    
        .navbar-nav li a:hover,
        .navbar-nav li.active a {
            color: #0d6efd !important;
            background-color: #fff !important;
        }
    
        .navbar-default .navbar-toggle {
            border-color: transparent;
            color: #fff !important;
        }
    
        .result-box {
            position: relative;
            width: 100%;
            height: calc(100vh - 50px);
            border: 1px solid #000;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: lightgreen;
        }
    
        .box-content {
            text-align: center;
            font-size: 36px;  /* Increased font size for better visibility */
            font-weight: bold;
        }
    
        .box-button {
            position: absolute;
            bottom: 10px;
            right: 10px;
            background-color: #0d6efd; /* Matching the navbar color */
            border: none;
            color: white;  /* White text color for contrast */
            padding: 10px 20px; /* Padding for better sizing */
            border-radius: 5px; /* Rounded corners */
            font-size: 16px; /* Font size matching the theme */
        }
    </style>
    
</head>

<body>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#myPage">Logo</a>
            </div>
            <div class="collapse navbar-collapse" id="myNavbar">
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="#À propos">À PROPOS</a></li>
                    <li><a href="#Matières">MATIÈRES</a></li>
                    <li><a href="#Services">SERVICES</a></li>
                    <li><a href="">INSCRIPTION</a></li>
                    <li><a href="">CONNEXION</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="result-box">
        <div class="box-content">
            <% if (scoreFinal !== undefined) { %>
                <p>Résultats : <%= scoreFinal %>/10</p>
            <% } else { %>
                <p>Pas de score disponible</p>
            <% } %>
        </div>
        <button class="box-button">Retour à l'accueil</button>
    </div>
</body>
</html>